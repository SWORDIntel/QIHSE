[package]
name = "qihse_rust"
version = "0.1.0"
edition = "2021"
description = "tokio-postgres-compatible Rust SDK for QIHSE"
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["full"] }
bytes = "1"
